# Scraping (ingestion locale uniquement)
playwright>=1.44.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Embeddings via Mistral API (remplace sentence-transformers)
openai>=1.30.0

# Vector store local
chromadb>=0.5.0

# API web
fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
